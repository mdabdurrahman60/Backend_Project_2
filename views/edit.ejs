<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <div class="nav mb-10">
            <a class="text-blue-500" href="/read">Update Users</a>
        </div>
        <h1 class="text-3xl tracking-tighter mb-3">Create User</h1>
        <hr>
        <form action="/update/<%= user._id %>" method="post">    
            <input class="px-5 mt-5 py-2 border-2 border-zinc-800 rounded-lg bg-transparent" type="text" placeholder="Name" name="name" value="<%= user.name%>">
            <input class="px-5 mt-5  py-2 border-2 border-zinc-800 rounded-lg bg-transparent" type="text" placeholder="E-mail" name="email" value="<%= user.email%>">
            <input class="px-5 mt-5  py-2 border-2 border-zinc-800 rounded-lg bg-transparent" type="text" placeholder="image url" name="image" value="<%= user.image%>">
            <input class="px-5 mt-5  py-2 bg-yellow-500 text-black rounded-lg ml-3" type="submit" value="Update User">
        </form>
    </div>
</body>
</html>